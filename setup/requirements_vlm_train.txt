# VLM QLoRA Training Dependencies (Phase A: Domain Adaptation)
# Lightweight: no DeepSpeed, Ray, or vLLM (not needed for single-GPU QLoRA)
#
# Used by: cai_integration/jobs_config_vlm.yaml
# GPU: T4 (16GB VRAM), single-GPU QLoRA with 4-bit quantization

# Core ML
torch>=2.2.0
torchvision>=0.17.0

# VLM fine-tuning
transformers>=4.57.0
accelerate>=1.0.0
peft>=0.14.0
bitsandbytes>=0.44.0
trl>=0.29.0

# Data
datasets>=2.16.0
sentencepiece
Pillow
tqdm
pyyaml

# Image processing
opencv-python
timm>=0.9.12

# Evaluation metrics
scikit-learn>=1.3.0
numpy>=1.24.0

# API (for eval with vLLM endpoint)
openai>=1.0.0
requests
