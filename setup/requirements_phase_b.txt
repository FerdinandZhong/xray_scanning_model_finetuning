# Phase B Combined Dependencies (YOLO + VLM QLoRA)
# Union of requirements_yolo.txt + requirements_vlm_train.txt
# Used by: cai_integration/jobs_config_vlm_phase_b.yaml
#          cai_integration/jobs_config_vlm_eval.yaml
#
# Phase B needs both ultralytics (class-agnostic YOLO) and
# transformers+peft+trl (proposal-guided VLM QLoRA fine-tuning)

# Core ML
torch>=2.2.0
torchvision>=0.17.0

# YOLO (class-agnostic object detection)
ultralytics>=8.0.0

# VLM fine-tuning
transformers>=4.57.0
accelerate>=1.0.0
peft>=0.14.0
bitsandbytes>=0.44.0
trl>=0.29.0

# Data
datasets>=2.16.0
sentencepiece
Pillow>=10.0.0
tqdm
pyyaml
numpy>=1.24.0

# Image processing
opencv-python
timm>=0.9.12

# Evaluation metrics
scikit-learn>=1.3.0
scipy>=1.11.0

# API and deployment
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6
openai>=1.0.0
requests

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# HiXray download fallback
gdown>=5.0.0

# Monitoring (optional)
prometheus-client>=0.19.0
